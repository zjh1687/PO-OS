import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SettingsPage() {
  return <PlaceholderPage title="Backup / Settings" description="2차 확장 범위입니다. JSON export/import, zod 검증, 전체 삭제 기능을 Supabase DB 기준으로 연결합니다." />;
}
